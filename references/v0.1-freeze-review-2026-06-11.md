# 资源管理 Agent v0.1 冻结前 Review（2026-06-11）

## 结论

当前版本可以进入 `v0.1 单节点 QA 通过版` 的冻结准备阶段。

本轮 review 的结论不是“可以直接切生产全自动”，而是：

- 单点能力仍然可独立调用。
- 配置与密钥已从可提交文件中拆出。
- 当前 Lark 表结构满足 Agent 运行依赖。
- v2 可见层/包装层没有取代原业务脚本。
- 冻结前还需要做一次人工 diff review 和提交分组，避免把中间探索内容混进稳定版本。

## 已通过检查

| 检查项 | 结果 |
| --- | --- |
| `git diff --check` | PASS |
| Python 编译检查 | PASS |
| 评分引擎测试 `tests/run_tests.py` | PASS，25/25 |
| Lark 表结构只读准入 `schema_validator.py --table all` | PASS，三张表均 0 缺失、0 疑似、0 类型不匹配 |
| 单节点 QA | PASS，见 `references/single-node-qa-report-2026-06-11.md` |
| 配置真实值复核 | PASS，新模板和文档未发现真实形态 Base token / App ID / API key 命中 |

## 本轮清理

- `config.yaml` 已停止作为可提交配置文件使用。
- 新增 `config.example.yaml` 作为 VM 安装模板。
- 本机真实配置保留在 `config.local.yaml`，并由 `.gitignore` 忽略。
- `HANDOVER.md`、`references/config.md`、`references/vm-setup.md` 已清理真实形态 table-id / App ID 示例。
- `schema_validator.py` 已修正为默认只读，不再在检查表头时强制写映射。
- `send_rejection_email.py --dry-run` 已修正为非交互可预览。
- `update_status.py` 已新增 `--dry-run`。

## 可提交分组建议

### A. 配置安全与安装门禁

建议作为第一组提交：

- `.gitignore`
- `config.example.yaml`
- 删除 Git 跟踪中的 `config.yaml`
- `scripts/config_loader.py`
- `scripts/check_config.py`
- `references/config-secrets-policy.md`
- `references/onboarding.md`
- `references/vm-setup.md`
- `references/config.md`
- `SKILL.md`
- `HANDOVER.md`

目的：先确保 skill 包不会带真实配置，并让 VM 安装路径清楚。

### B. Lark 表结构准入与字段映射治理

建议作为第二组提交：

- `references/lark-required-schema.yaml`
- `references/lark-field-dictionary.md`
- `scripts/schema_inspector.py`
- `scripts/schema_validator.py`
- `scripts/schema_gate.py`
- `scripts/field_resolver.py`
- `scripts/lark_cli_utils.py`
- `scripts/integration_readiness.py`
- `scripts/regression_report.py`

目的：把“换表前先识别表头、缺什么列、哪些列用途是什么、确认后再新增/映射”的流程固化。

### C. 单节点稳定性修复

建议作为第三组提交：

- `scripts/parse_resumes.py`
- `scripts/evaluate_resumes.py`
- `scripts/rescore_and_write.py`
- `scripts/send_test_email.py`
- `scripts/generate_contract.py`
- `scripts/check_signed_contract.py`
- `scripts/send_rejection_email.py`
- `scripts/update_status.py`
- `scripts/export_badcase_snapshots.py`
- `scripts/field_mapping.py`

目的：保留原单点脚本独立可用，同时补配置读取、字段映射、dry-run、TEST_MODE、非交互预览等生产化保护。

### D. 过程可见层与 Demo 工具

建议作为第四组提交，且提交信息要明确“旁路观测，不接管主流程”：

- `scripts/workflow_engine.py`
- `scripts/workflow_runner.py`
- `scripts/run_dialog.py`
- `scripts/rescore_and_write_v2.py`
- `scripts/send_test_email_v2.py`
- `scripts/generate_contract_v2.py`
- `scripts/manual_trace.py`
- `scripts/run_testmode_demo.py`
- `references/integration-validation-plan.md`
- `references/manual-chain-runbook.md`
- `references/original-agent-baseline-audit.md`
- `references/stable-qa-checklist.md`
- `references/single-node-qa-report-2026-06-11.md`

目的：提供过程可见、QA、录制证据，不改变原脚本业务判断。

## 不应提交的内容

- `config.local.yaml`
- `config.yaml`
- `config/lark-field-mapping.yaml`
- `.env` / `.env.*`
- 任何真实 SMTP 密码、LLM API key、Lark app secret、GitHub token
- 本机生成的 PDF 图片缓存、合同输出、邮件草稿、临时截图/录屏

## 冻结前人工确认点

1. 是否接受当前版本名称为 `v0.1 单节点 QA 通过版`。
2. 是否先按 A/B/C/D 四组拆提交，而不是一次性提交全部改动。
3. 是否保留 `workflow_runner next`，但继续标注为非默认生产入口。
4. 是否把旧状态「📋 新投递」迁移或兼容到新状态「📋 简历待筛选」。
5. 是否开启 Badcase GitHub issue 回流；当前仅 dry-run 通过，正式回流还需要 `badcase_export.enabled` 和 gh 登录。

## 下一步建议

先做一次人工 diff review，然后按 A/B/C/D 分组提交。

提交完成后再做两件事：

1. 打包 skill，确认包内只有 `config.example.yaml`，没有真实配置。
2. 基于 `single-node-qa-report-2026-06-11.md` 录制真实 TEST_MODE 执行路径 Demo。
